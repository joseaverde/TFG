package Detector.Details.Simpson with SPARK_Mode => On is

   function Simpson (
      Signal : in Feature_Array;
      dx     : in Feature_Type)
      return Feature_Type;

end Detector.Details.Simpson;
